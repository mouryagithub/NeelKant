// Fibonacci example
function fibonacci(n: int): int {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

function main(): void {
    var result: int = fibonacci(10);
    print(result);
}
